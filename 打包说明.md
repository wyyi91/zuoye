# PID温度控制仿真系统 - 打包发布指南

## 环境要求

- Python 3.10+
- Windows 10/11

## 安装依赖

```bash
pip install -r requirements.txt
```

## 开发运行

```bash
python src/main.py
```

## 打包为单文件EXE

### 方式一：使用 build_exe.py（推荐）

```bash
python build_exe.py
```

### 方式二：直接使用 PyInstaller

```bash
pyinstaller PID仿真系统.spec
```

### 方式三：命令行打包

```bash
pyinstaller --onefile --windowed --name "PID温度控制仿真系统" --add-data "assets;assets" --add-data "data;data" src/main.py
```

## 输出文件

打包成功后，可执行文件位于：

```
dist/PID温度控制仿真系统.exe
```

## 发布包结构

```
发布包/
├── PID温度控制仿真系统.exe    # 主程序（免安装）
├── data/                      # 数据目录（首次运行自动创建）
│   ├── history_data/          # 历史仿真数据
│   ├── users/                 # 用户数据
│   └── logs/                  # 日志文件
└── 使用说明.txt               # 使用说明
```

## 注意事项

1. 打包前确保所有依赖已安装
2. 首次运行时，程序会自动创建 data 目录
3. 默认账号: admin/admin123 和 user/user123
4. 建议在 Windows 10/11 64位系统上运行
